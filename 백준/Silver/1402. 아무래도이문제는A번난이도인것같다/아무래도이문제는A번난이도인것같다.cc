#include <iostream>

using namespace std;

int main(void) {
    int t, a, b;
    cin >> t;
    
    while (t--) {
        cin >> a >> b;
        cout << "yes" << endl;
    }
    
    return 0;
}
